export type Entity = {
  id: number;
  name: string;
};

export type Coords = {
  id: number;
  x: number;
  y: number;
};

export type EntityObject = {
  id?: number;
  name: string;
  x: number;
  y: number;
};
