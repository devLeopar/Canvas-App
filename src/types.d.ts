export type EntityData = {
  id: number;
  name: string;
};

export type CoordsData = {
  id: number;
  x: number;
  y: number;
};

export type EntityObject = {
  id?: number;
  name: string;
  x: number;
  y: number;
};
